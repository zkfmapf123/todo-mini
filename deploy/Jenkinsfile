pipeline {
    agent any

    stages {
        stage("CI Test") {
            steps {
                echo "ci test"
            }
        }

        stage("Stress Test") {
            steps {
                echo "stress test"
            }
        }

        stage("Build") {
            steps {
                echo "build"
            }
        }

        stage("Push The ECR") {
            steps {
                echo "push the ecr"
            }
        }

        stage("Send Notification") {
            steps {
                echo "send notification"
            }
        }
    }
}