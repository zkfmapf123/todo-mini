pipeline{
    agent any

    tools {nodejs "nodejs"}

    stages {
        stage("node install") {
            steps {
                docker {
                    image 'node'
                }
            }
        }

        steps {
            sh "docker images"
        }
    }
}