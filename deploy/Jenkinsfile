pipeline{
    agent any

    stages {
        stage("node install") {
            steps {
                docker {
                    image 'node'
                }
            }
        }

        steps {
            sh "docker images"
        }
    }
}